<% @task_lists.each do |task_list| %>
  <% if flash[:notice] %>
    <%= flash[:notice] %>
    <% end %>

  <section class="task-list">
    <h1><%= task_list.name %></h1>

    <div class="<%= task_list.name %>">
      <%= link_to 'New Task', new_task_list_task_path(task_list) %>
    </div>

    <% task_list.tasks.each do |task| %>
      <%= task.description %>
      <%= task.due_date %>
    <% end %>
  </section>


<% end %>